#include <iostream>
#include <bits/stdc++.h>
using namespace std;
int main(){
     int n; cin>>n;   // root number 
     int m; cin>>m;   // root of 
     int a=m/n;
     vector<int>vec;
     for (int i=2; i<=a; i++){
          vec.push_back(i);
     }
     int b=vec.size()/2;
     while (1){
          if (vec[b])
     }


     
}